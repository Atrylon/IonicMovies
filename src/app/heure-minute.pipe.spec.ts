import { HeureMinutePipe } from './heure-minute.pipe';

describe('HeureMinutePipe', () => {
  it('create an instance', () => {
    const pipe = new HeureMinutePipe();
    expect(pipe).toBeTruthy();
  });
});
